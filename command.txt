docker ps 
docker ps -a
docker rm
docker start 
docker run --name new_name -d docker_name
docker rename old new
docker system prune -a
docker exec
docker -it container_name
ctrl + p + q -> to exit from docker without turn off
docker attach container_name
docker exec container_id cat /etc/issue
docker build -t <image_name>:<tag> <path_to_dockerfile>      docker build -t myapp:1.0 .
docker inspect
docker network ls
docker run -d --network mynet nginx
docker network create name --subnet 10.0.0.0/8 --driver bridge
docker run -- name bridge -- network subnet-nw -d nginx
docker network connect mynet1 c2
docker images -q --filter "dangling=true" | xargs docker rmi
docker run -d --name my-nginx -p 80:80 nginx
docker start -ai [container_name_or_id]
docker run -v /host/path:/app my_image
docker stack deploy -c stack.yaml
docker node ls
docker node service
docker node promote
docker node demote
docker service rollback
docker compose.yml
docker-compose up, logs, run
docker service ps service_name
docker save -o <image_name>.tar <image_name>
docker load -i <image_name>.tar
